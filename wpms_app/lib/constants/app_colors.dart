import 'package:flutter/material.dart';

class AppColors {
  static const orange = Color(0xFFEE5F2D);
  static const dark = Color(0xFF1A1A1A); // confirm exact hex from Figma
  static const background = Colors.white; // confirm exact base color
}
